defaults:
  - /model/tarokk@model: small
  - _self_

seed: 42
num_iterations: 10
exploration: 0.3

sampler:
  advantage_capacity: 10_000_000
  strategy_capacity: 10_000_000
  num_traversals: 10_000
  batch_size: 4096
  device: "cuda"

training:
  lr: 5e-5

data:
  advantage:
    train_split: 0.9
    batch_size: 3500
    workers: 4
  strategy:
    train_split: 0.9
    batch_size: 256
    workers: 4
