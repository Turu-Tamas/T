[project]
name = "t"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13,<3.14"
dependencies = [
    "open-spiel",
    "jupyterlab",
    "numpy",
    "ipykernel",
    "matplotlib",
    "torch"
]

[tool.uv.sources]
# Prebuilt wheel of the personal fork (github.com/Turu-Tamas/open_spiel).
# Rebuild after updating the fork:  see wheels/README.md
open-spiel = { path = "wheels/open_spiel-1.6.15-cp313-cp313-linux_x86_64.whl" }
